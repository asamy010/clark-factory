# CLARK Factory Management — V19.52

**التاريخ:** 2026-05-04
**النوع:** تغيير معماري + إكمال
**Build status:** ✅ نجح (9.35s، صفر errors)
**Pre-flight audit:** ✅ نظيف — مفيش external endpoint بيقرا الحقول الـ4

---

## 🎯 الهدف
آخر الـ4 arrays operational في `factory/config` (بعد V19.50). بعد V19.52 = factory/config مفيهوش حقل operational يكبر، باقي بس notifications اللي مؤجلة لـV19.53 (تحتاج refactor مسبق لـreadBy/dismissedBy).

## ✅ المُقسَّم في V19.52
| الحقل | Collection | معدل النمو |
|---|---|---|
| **stockMovements** | stockMovementsDays/{YYYY-MM-DD} | عالي جداً |
| **purchaseReceipts** | purchaseReceiptsDays/{YYYY-MM-DD} | يومي |
| **treasuryTransfers** | treasuryTransfersDays/{YYYY-MM-DD} | أحياناً |
| **salesAudits** | salesAuditsDays/{YYYY-MM-DD} | أحياناً |

## 🔧 التغييرات

### 1. `src/utils/splitCollections.js`
- + `SPLIT_FIELDS_V1952` + `SPLIT_FLAG_V1952`
- + 4 entries في `SPLIT_COLLECTIONS`
- `stripSplitArrays` بقت تستوعب 4 مجموعات flags

### 2. `src/App.jsx`
- import `SPLIT_FIELDS_V1952` + `SPLIT_FLAG_V1952`
- `config` useMemo: gating V19.52 fields على الـflag
- upConfig hydration: V19.52 fields من splitData لو الـflag on
- Migration block جديد (~95 سطر) — backup → sync → atomic strip + flag

### 3. `firestore.rules`
- + 4 collections بصلاحيات مطابقة للأصلية:
  - stockMovementsDays / purchaseReceiptsDays / treasuryTransfersDays (purchase scope)
  - salesAuditsDays (sales scope)
- Header: V19.52

### 4. `src/utils/sizeBudget.js`
- + 4 entries جديدة بـ`dataType:"split"`

### 5. `src/pages/SettingsPg.jsx` — لوحة المراقبة
- `collectionMeta` بقت 19 entry
- العنوان: "📅 مراقبة التخزين اليومي (V16.74 → V19.52)"

### 6. `src/constants/index.js` + `src/components/AboutVersionModal.jsx` + `package.json`
- APP_VERSION = "V19.52"
- changelog entry جديد
- 19.52.0

---

## ⚙️ سلوك الـMigration
1. المستخدم يفتح التطبيق على V19.52 لأول مرة
2. flags الموجودة: V1674/V1949/V1950/sales1951/tasks1951 = true. flag الجديد: undefined
3. listeners الـ4 الجدد تشتغل (فاضية مبدئياً)
4. config useMemo: V19.52 fields لسه من configDoc
5. Migration بيلاقي 4 arrays → backup كامل → نقل لـday docs → strip + flag (transaction atomic)
6. configDoc snapshot يصل بدون الـ4 arrays
7. config useMemo بقى يعرضها من splitData
8. الصفحات (PurchasePg, WarehousePg, TreasuryPg, CustDeliverPg) شغّالة عادي **بدون أي تعديل**

## 🛡️ آليات الأمان (مكرّرة)
- ✅ Pre-flight audit للـAPI/services (الدرس من V19.51 hotfix)
- ✅ Backup كامل لـconfig قبل أي تعديل
- ✅ Transaction atomic للـstrip + flag
- ✅ Selective stripping بـflag
- ✅ Recovery: لو فشلت → flag مش بيتكتب → retry تلقائي

## 📝 خطة الاختبار
1. **افتح التطبيق** — V19.52 في التوب بار + console: `[CLARK V19.52]`
2. **Migration modal**: "جاري تحديث نظام التخزين (V19.52)" → 100%
3. **Firestore Console:**
   - factory/config: مفيش `stockMovements/purchaseReceipts/treasuryTransfers/salesAudits` + `_splitDaysV1952Done = true`
   - 4 collections جديدة فيهم بيانات
   - `backups/pre-migration-split-days-v1952-*` موجود
4. **التطبيق:**
   - WarehousePg → استلم خامة جديدة → stockMovement يضاف
   - PurchasePg → اعمل receipt جديد → purchaseReceipt يضاف
   - TreasuryPg → حوّل بين حسابين → treasuryTransfer يضاف
   - CustDeliverPg → اعمل جرد مبيعات → salesAudit يضاف
5. **Settings → "📅 مراقبة التخزين اليومي"** — 19 collection بـbadges صح

## 🔄 Rollback
- Rollback التطبيق لـV19.51 على Vercel
- Restore: `setDoc(doc(db,"factory","config"), backup.config)` من `backups/pre-migration-split-days-v1952-*`
- احذف 4 collections اللي اتولدت

## 🎯 الجاي (V19.53)
- **notifications** بـrefactor مسبق لـ`readBy/dismissedBy` (sub-collection per user) → split آمن
- **Master data byId partitioning** (V19.54+): customers / suppliers / workshops / employees / generalProducts

## ✅ Deployment Checklist
- [ ] Backup يدوي إضافي
- [x] **`npm run build`** ✅ (9.35s)
- [ ] Deploy على Vercel preview أولاً
- [ ] Test: console `[CLARK V19.52]` + topbar V19.52
- [ ] Promote للـproduction
- [ ] راقب أول 30 دقيقة:
  - `migrationLog/split-days-v19.52-*` بـstatus=success
  - factory/config size نزل
  - 4 collections جديدة بدأت تتملي
- [ ] Console errors في الـ24 ساعة الأولى
