# CLARK Factory Management — V19.57

**التاريخ:** 2026-05-04
**النوع:** architectural — الـMaster Data byId Partitioning
**Build status:** ✅ نجح (10.03s، صفر errors + API syntax pass)
**Pre-flight audit:** ✅ — 4 API endpoints محدّثين

---

## 🎯 الهدف

تحويل آخر 8 arrays في `factory/config` لـbyId collections منفصلة. بعد V19.57 = الـArchitecture اكتمل بالكامل، مفيش doc واحد في النظام ممكن يكبر مهما طال الوقت.

---

## 📊 الـMigration

### الـ8 collections الجديدة
| الحقل | Collection | الصلاحيات |
|---|---|---|
| **customers** | `customersDocs/{id}` | sales scope writes |
| **suppliers** | `suppliersDocs/{id}` | purchase scope |
| **workshops** | `workshopsDocs/{id}` | manager+ |
| **employees** | `employeesDocs/{id}` | HR scope |
| **empDebts** | `empDebtsDocs/{id}` | HR scope |
| **generalProducts** | `generalProductsDocs/{id}` | purchase scope |
| **fabrics** | `fabricsDocs/{id}` | manager+ |
| **accessories** | `accessoriesDocs/{id}` | manager+ |

### قبل/بعد
```
قبل V19.57:                                بعد V19.57:
factory/config = {                         factory/config = {
  customers: [35],         ⚠️                /* arrays محذوفة */
  suppliers: [21],         ⚠️                _partitionedV1957Done: true,
  workshops: [17],         ⚠️                ...settings + lookup tables ثابتة
  employees: [67],         ⚠️              }
  empDebts: [11],          ⚠️
  generalProducts: [...],  ⚠️              + 8 collections × N docs:
  fabrics: [...],          ⚠️                customersDocs/{cust_xxx}
  accessories: [...],      ⚠️                suppliersDocs/{sup_xxx}
  ...settings              ✅                workshopsDocs/{1, 2, 3, ...}
}                                            ...
```

---

## ✅ الـ Architecture النهائي

```
factory/config  → settings + lookup tables بس (ثابت ~30 KB)
factory/sales   → settings فقط (ثابت ~5 KB)
factory/tasks   → settings فقط (ثابت ~5 KB)
─────────────────────────────────────────────────
+ 20 daily-split collections (treasury, payments, invoices, etc.)
+ 9 byId-partitioned collections (hrWeeks + 8 master data)
+ userNotifStates/{email}
─────────────────────────────────────────────────
= مفيش doc واحد ممكن يكبر مهما طال الوقت
```

**الضمان الرياضي اكتمل بالكامل** ✅

---

## 🔧 Files Modified

| File | Change |
|---|---|
| `src/utils/partitionedCollections.js` | + 8 fields في PARTITIONED_COLLECTIONS، + V1957 group + flag، selective stripping، generic stats labels |
| `src/App.jsx` | dynamic listeners (PARTITIONED_FIELDS loops)، hydration with V1675/V1957 gating، migration block جديد، localStorage snapshot reads from `config` (merged) |
| `api/_firebase.js` | + `readPartitionedCollection` helper (byId pattern, returns array of entities) |
| `api/customer-portal.js` | reads customers from `customersDocs` (with fallback) |
| `api/delivery-confirm.js` | نفس |
| `api/workshop-portal.js` | reads workshops from `workshopsDocs` (with fallback) |
| `api/workshop-delivery-confirm.js` | نفس |
| `firestore.rules` | + 8 collections + version V19.57 |
| `src/utils/sizeBudget.js` | 5 master-data entries بقت `dataType:"partitioned"` بـbudgets كبيرة |
| `src/pages/SettingsPg.jsx` | PartitionedDocsMonitor بقى 9 collections + version badges + generic labels |
| `src/constants/index.js` | APP_VERSION = "V19.57" |
| `src/components/AboutVersionModal.jsx` | + V19.57 changelog (8 changes) |
| `package.json` | 19.57.0 |
| `docs/V19.57.md` | this file |

**ملفات لم تتعدّل (transparent):**
- `src/pages/DBPg.jsx` — قراءة + كتابة customers/suppliers/workshops شغّالة بدون تعديل
- `src/pages/HRPg.jsx` — employees + empDebts شغّالة
- `src/pages/WarehousePg.jsx` — fabrics + accessories + generalProducts شغّالة
- `src/pages/CustDeliverPg.jsx` — customers reads transparent
- `src/pages/PurchasePg.jsx` — suppliers reads transparent
- `src/pages/ExtProdPg.jsx` — workshops reads transparent
- `src/pages/SettingsPg.jsx` — مفيش تغيير في الـlogic، فقط الـmonitor card

السبب: الـ`config` useMemo بيـmerge partitionedData[f] فوق configDoc[f] لما الـflag set. كل الصفحات بتقرا من `data.customers` (الـmerged) فبتشوف الـarray كامل.

---

## ⚙️ سلوك الـMigration

1. المستخدم يفتح التطبيق على V19.57 لأول مرة
2. flags الموجودة: `_partitionedV1675Done=true` (من قبل). flag الجديد: `_partitionedV1957Done=undefined`
3. listeners تشتغل على 9 collections (1 hrWeeks موجودة + 8 جديدة فاضية)
4. Migration بيلاقي 8 arrays في configDoc → بيتأكد كل entity فيها `id` (يولد لو لازم)
5. backup كامل في `backups/pre-migration-partitioned-v1957-{ts}`
6. كل entity يكتب كـdoc منفصل (parallel writes within collection، sequential across)
7. transaction atomic: strip 8 arrays + flag = true
8. configDoc snapshot يصل بدون الـ8 arrays
9. config useMemo بقى يعرضهم من partitionedData
10. كل الصفحات شغّالة عادي **بدون أي تعديل**

---

## 📝 خطة الاختبار

### حرج
1. **افتح التطبيق** — V19.57 في التوب بار + console: `[CLARK V19.57]`
2. **Migration modal** — "جاري تحديث نظام التخزين (V19.57)" → 100%
3. **Firestore Console:**
   - `factory/config` — مفيش `customers/suppliers/workshops/employees/empDebts/generalProducts/fabrics/accessories`
   - `factory/config._partitionedV1957Done = true`
   - 8 collections جديدة فيهم docs (مثلاً `customersDocs` بـ35 doc، `employeesDocs` بـ67 doc...)
   - `backups/pre-migration-partitioned-v1957-*` موجود

### مهم
4. **DBPg** → افتح صفحة العملاء → الكل ظاهر → عدّل عميل → احفظ → Refresh → التعديل ثابت
5. **HRPg** → افتح موظف → عدّل بيانات → احفظ → ثابت
6. **WarehousePg** → عدّل قماش → ثابت
7. **CustDeliverPg** → اعمل توزيعة لعميل → التوزيعة + الدفعات شغّالة
8. **PurchasePg** → اعمل أمر شراء لمورد → شغّال
9. **ExtProdPg** → اعمل تسليم لورشة → شغّال
10. **Customer portal link** — افتح اللينك بتاع عميل → بياناته ظاهرة (API يقرا من customersDocs)
11. **Workshop portal link** — نفس الكلام

### Settings
12. Settings → "📑 مراقبة الـDocuments المُجزّأة" → 9 collections مع badges (V16.75 + V19.57)
13. كل collection يعرض count + total size + avg per doc

---

## 🔄 Rollback

1. Rollback التطبيق لـV19.56 على Vercel
2. **Restore master data:**
   - افتح `backups/pre-migration-partitioned-v1957-{ts}`
   - استعد عبر: `setDoc(doc(db,"factory","config"), backup.config)`
   - احذف 8 collections الجديدة (customersDocs, suppliersDocs, ...)

---

## 🎯 الجاي

**V19.58+** ممكن يكون:
- ARCHITECTURE.md document — توثيق رسمي للـpatterns + الـrules للمطورين
- Auto-warning في Settings لو حقل وصل >100KB (preventive)
- أو bug fixes / features من production feedback

---

## ✅ Deployment Checklist

- [ ] Backup يدوي إضافي
- [x] **`npm run build`** ✅ (10.03s)
- [x] **API syntax check** ✅ (`node --check`)
- [ ] Deploy على Vercel preview أولاً
- [ ] Test: console `[CLARK V19.57]` + topbar V19.57
- [ ] Promote للـproduction
- [ ] راقب أول 30 دقيقة:
  - `migrationLog/partitioned-v19.57-*` بـstatus=success
  - factory/config مفيهوش الـ8 arrays
  - 8 collections جديدة بدأت تتعمر
- [ ] Test كل صفحة (DB, HR, Warehouse, CustDeliver, Purchase, ExtProd) بـreal data
- [ ] Test customer + workshop portal links
- [ ] Console errors في الـ24 ساعة الأولى
